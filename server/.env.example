# JWT密钥 - 用于生成用户登录token
# ⚠️ 重要: 请务必修改为随机字符串(建议32字符以上)
# 生成方法:
#   方法1: 使用在线工具 https://randomkeygen.com/
#   方法2: PowerShell命令
#          -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
#   方法3: Linux/Mac命令
#          openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 环境
NODE_ENV=production

# 端口
PORT=3001
